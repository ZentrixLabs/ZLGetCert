<Window x:Class="ZLGetCert.Views.ConfigurationEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration Editor" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Icon="../ZLGetCert_icon_v2.ico">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#6B7280"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4B5563"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#374151"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="WarningTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="20,15">
            <StackPanel>
                <TextBlock Text="⚙️ Configuration Editor" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Edit appsettings.json directly. Changes will require an application restart to take effect." 
                           Style="{StaticResource InfoTextStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- JSON Editor -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0">
                <TextBlock Text="⚠️ Warning" Style="{StaticResource WarningTextStyle}"/>
                <TextBlock Text="• Invalid JSON will prevent the application from starting&#x0a;• Always backup your configuration before making changes&#x0a;• Test your changes in a development environment first" 
                           Style="{StaticResource InfoTextStyle}"/>
            </StackPanel>
            
            <!-- Validation Status -->
            <Border Grid.Row="1" 
                    Background="{Binding ValidationBackground}" 
                    BorderBrush="{Binding ValidationBorderBrush}"
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Padding="12" 
                    Margin="0,10,0,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ValidationIcon}" 
                                   FontSize="16" 
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ValidationMessage}" 
                                   FontWeight="SemiBold" 
                                   Foreground="{Binding ValidationForeground}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ValidationDetails}" 
                               Style="{StaticResource InfoTextStyle}"
                               Margin="0,5,0,0"
                               Visibility="{Binding HasValidationDetails, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Border>
            
            <TextBox Grid.Row="2" 
                     x:Name="JsonEditor"
                     Text="{Binding ConfigurationJson, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Consolas"
                     FontSize="12"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     Margin="0,10,0,0"
                     Padding="10"
                     Background="#F9FAFB"
                     BorderBrush="#D1D5DB"
                     BorderThickness="1"/>
        </Grid>
        
        <!-- Footer with Buttons -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="📁 Load from File" 
                        Command="{Binding LoadFromFileCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"/>
                <Button Content="💾 Save to File" 
                        Command="{Binding SaveToFileCommand}"
                        Style="{StaticResource ButtonStyle}"/>
                <Button Content="🔄 Reset to Default" 
                        Command="{Binding ResetToDefaultCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"/>
                <Button Content="✅ Apply &amp; Close" 
                        Command="{Binding ApplyAndCloseCommand}"
                        Style="{StaticResource ButtonStyle}"/>
                <Button Content="❌ Cancel" 
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
